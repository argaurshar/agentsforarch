# AND Studio runs fully with ZERO configuration against the built-in mock
# provider. These keys are only needed to wire a real image provider later
# (see build spec §5). Copy this file to `.env` and fill in a key to activate
# the corresponding provider; the app auto-selects the first configured one.

# VITE_MAGNIFIC_KEY=
# VITE_FLUX_KEY=
